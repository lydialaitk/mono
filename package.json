{
  "name": "mono",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "type": "commonjs",
  "scripts": {
    "dev": "concurrently \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "build": "npm -w shared run build && npm -w frontend run build && npm -w backend run build",
    "start": "node backend/dist/src/app/main.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint .",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "@eslint/js": "^9.9.0",
    "concurrently": "^9.0.1",
    "eslint": "^9.9.0",
    "jsdom": "^25.0.1",
    "prettier": "^3.3.3",
    "typescript-eslint": "^8.2.0",
    "vitest": "^3.0.0"
  }
}

